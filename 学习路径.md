# 巴西电商数据学习路径（按你当前进度）

## 0. 你现在的真实进度

从当前工作区来看，你已经完成了第一段里最难的工程部分：

- 已经下载了 Olist 全量 CSV
- 已经写了 Python 导入脚本
- 已经不再依赖 `to_sql(replace)` 自动建表
- 已经改成了 `显式 CREATE TABLE + to_sql(append)` 的可靠方案
- 已经开始思考主键、外键、类型映射、空值、孤儿值这些问题

这说明你现在**不该继续停留在“怎么导入 CSV”**，而应该进入下面这条主线：

`导入成功 -> 导入可信 -> 会做 SQL -> 会回答业务问题 -> 会输出分析报告`

下面的学习路径就是按这条主线展开。

---

## 1. 第一阶段：把“导入成功”升级为“导入可信”

这一阶段不要急着刷复杂 SQL，先把库验清楚。

你的目标：

1. 确认表都在
2. 确认行数合理
3. 确认主键粒度没错
4. 确认关键连接字段能对上
5. 确认时间和金额字段可以正常分析

### 1.1 先跑导入脚本

```python
# run_import_olist_to_mysql.py
from importlib.util import module_from_spec, spec_from_file_location
from pathlib import Path


def load_and_run():
    script_path = Path(__file__).resolve().parent / "format" / "import_olist_to_mysql.py"
    spec = spec_from_file_location("import_olist_to_mysql", script_path)
    module = module_from_spec(spec)
    if spec.loader is None:
        raise RuntimeError(f"Unable to load script: {script_path}")
    spec.loader.exec_module(module)
    module.main()


if __name__ == "__main__":
    load_and_run()
```

运行方式：

```powershell
python .\run_import_olist_to_mysql.py
```

### 1.2 检查有哪些表

```sql
USE olist_db;

SHOW TABLES;
```

### 1.3 检查每张表的行数

先练最基本的“落库验收”：

```sql
SELECT COUNT(*) AS row_cnt FROM olist_orders_dataset;
SELECT COUNT(*) AS row_cnt FROM olist_order_items_dataset;
SELECT COUNT(*) AS row_cnt FROM olist_order_payments_dataset;
SELECT COUNT(*) AS row_cnt FROM olist_order_reviews_dataset;
SELECT COUNT(*) AS row_cnt FROM olist_customers_dataset;
SELECT COUNT(*) AS row_cnt FROM olist_products_dataset;
SELECT COUNT(*) AS row_cnt FROM olist_sellers_dataset;
```

### 1.4 检查主键是否符合预期

#### 订单表：`order_id` 应该唯一

```sql
SELECT
    COUNT(*) AS total_rows,
    COUNT(DISTINCT order_id) AS distinct_order_id
FROM olist_orders_dataset;
```

#### 订单明细表：主键应该是 `(order_id, order_item_id)`
>使用CONCAT进行拼接来体现复合主键性质

```sql
SELECT
    COUNT(*) AS total_rows,
    COUNT(DISTINCT CONCAT(order_id, '-', order_item_id)) AS distinct_pk
FROM olist_order_items_dataset;
```

#### 支付表：主键应该是 `(order_id, payment_sequential)`

```sql
SELECT
    COUNT(*) AS total_rows,
    COUNT(DISTINCT CONCAT(order_id, '-', payment_sequential)) AS distinct_pk
FROM olist_order_payments_dataset;
```

### 1.5 检查关键外键能不能接上

#### orders -> customers

```sql
SELECT
    COUNT(*) AS total_order_rows,
    SUM(CASE WHEN c.customer_id IS NULL THEN 1 ELSE 0 END) AS unmatched_customer_rows
FROM olist_orders_dataset o
LEFT JOIN olist_customers_dataset c
    ON o.customer_id = c.customer_id;
```

#### order_items -> orders

```sql
SELECT
    COUNT(*) AS total_item_rows,
    SUM(CASE WHEN o.order_id IS NULL THEN 1 ELSE 0 END) AS unmatched_order_rows
FROM olist_order_items_dataset i
LEFT JOIN olist_orders_dataset o
    ON i.order_id = o.order_id;
```

#### order_items -> products

```sql
SELECT
    COUNT(*) AS total_item_rows,
    SUM(CASE WHEN p.product_id IS NULL THEN 1 ELSE 0 END) AS unmatched_product_rows
FROM olist_order_items_dataset i
LEFT JOIN olist_products_dataset p
    ON i.product_id = p.product_id;
```

### 1.6 检查关键字段空值

```sql
SELECT
    SUM(CASE WHEN order_id IS NULL THEN 1 ELSE 0 END) AS null_order_id,
    SUM(CASE WHEN customer_id IS NULL THEN 1 ELSE 0 END) AS null_customer_id,
    SUM(CASE WHEN order_status IS NULL THEN 1 ELSE 0 END) AS null_order_status,
    SUM(CASE WHEN order_purchase_timestamp IS NULL THEN 1 ELSE 0 END) AS null_purchase_time,
    SUM(CASE WHEN order_estimated_delivery_date IS NULL THEN 1 ELSE 0 END) AS null_estimated_delivery
FROM olist_orders_dataset;
```

### 1.7 这一阶段你要真正掌握什么

- `COUNT(*)`总行数正确
- `COUNT(DISTINCT ...)`主键具有唯一性，如果是复合主键则使用CONCAT
- `LEFT JOIN`外键连接没有问题
- `CASE WHEN`判断非空行个数

---

## 2. 第二阶段：数据审计，先把口径练扎实

你现在最该练的不是花哨 SQL，而是“口径控制”。

这套数据最容易出错的地方：

- `orders` 是订单粒度
- `order_items` 是订单商品粒度
- `payments` 是订单支付明细粒度
- `reviews` 是订单评价粒度

如果直接把 `orders + order_items + payments` 一把 join，到最后金额很容易被重复放大。

### 2.1 检查订单金额口径

先从 `order_items` 口径算订单金额：

```sql
SELECT
    order_id,
    SUM(price) AS item_amount,
    SUM(freight_value) AS freight_amount,
    SUM(price + freight_value) AS total_item_amount
FROM olist_order_items_dataset
GROUP BY order_id
LIMIT 10;
```

再从 `payments` 口径看订单支付金额：

```sql
SELECT
    order_id,
    SUM(payment_value) AS total_payment_amount
FROM olist_order_payments_dataset
GROUP BY order_id
LIMIT 10;
```

然后做对账：

```sql
WITH item_amount AS (
    SELECT
        order_id,
        SUM(price + freight_value) AS item_total
    FROM olist_order_items_dataset
    GROUP BY order_id
),
payment_amount AS (
    SELECT
        order_id,
        SUM(payment_value) AS payment_total
    FROM olist_order_payments_dataset
    GROUP BY order_id
)
SELECT
    i.order_id,
    i.item_total,
    p.payment_total,
    ROUND(i.item_total - p.payment_total, 2) AS diff_amount
FROM item_amount i
JOIN payment_amount p
    ON i.order_id = p.order_id
WHERE ABS(i.item_total - p.payment_total) > 0.01
LIMIT 20;
```

这一步很关键。它会逼你理解：

- 指标从哪个表出
- 聚合要先做还是后做
- 一对多 join 为什么会把数乘大

### 2.2 检查时间逻辑

```sql
SELECT
    COUNT(*) AS abnormal_rows
FROM olist_orders_dataset
WHERE order_approved_at IS NOT NULL
  AND order_approved_at < order_purchase_timestamp;
```

```sql
SELECT
    COUNT(*) AS abnormal_rows
FROM olist_orders_dataset
WHERE order_delivered_customer_date IS NOT NULL
  AND order_delivered_customer_date < order_purchase_timestamp;
```

### 2.3 检查已交付订单的配送时长

```sql
SELECT
    order_id,
    TIMESTAMPDIFF(
        DAY,
        order_purchase_timestamp,
        order_delivered_customer_date
    ) AS delivery_days
FROM olist_orders_dataset
WHERE order_status = 'delivered'
  AND order_delivered_customer_date IS NOT NULL
LIMIT 20;
```

### 2.4 这一阶段你要掌握什么

- 先聚合再 join
- 指标口径统一
- 时间字段逻辑检查
- 对账思维

---

## 3. 第三阶段：单表 SQL 基础题，先把订单主表吃透

现在开始进入正式 SQL 训练，但不要上来就多表。

### 3.1 订单总数

```sql
SELECT COUNT(*) AS total_orders
FROM olist_orders_dataset;
```

### 3.2 各订单状态分布

```sql
SELECT
    order_status,
    COUNT(*) AS order_cnt
FROM olist_orders_dataset
GROUP BY order_status
ORDER BY order_cnt DESC;
```

### 3.3 月度订单趋势

```sql
SELECT
    DATE_FORMAT(order_purchase_timestamp, '%Y-%m') AS month,
    COUNT(*) AS order_cnt
FROM olist_orders_dataset
GROUP BY DATE_FORMAT(order_purchase_timestamp, '%Y-%m')
ORDER BY month;
```

### 3.4 月度已交付订单趋势

```sql
SELECT
    DATE_FORMAT(order_purchase_timestamp, '%Y-%m') AS month,
    COUNT(*) AS delivered_order_cnt
FROM olist_orders_dataset
WHERE order_status = 'delivered'
GROUP BY DATE_FORMAT(order_purchase_timestamp, '%Y-%m')
ORDER BY month;
```

### 3.5 月度取消率

```sql
SELECT
    DATE_FORMAT(order_purchase_timestamp, '%Y-%m') AS month,
    COUNT(*) AS total_orders,
    SUM(CASE WHEN order_status = 'canceled' THEN 1 ELSE 0 END) AS canceled_orders,
    ROUND(
        SUM(CASE WHEN order_status = 'canceled' THEN 1 ELSE 0 END) / COUNT(*),
        4
    ) AS canceled_rate
FROM olist_orders_dataset
GROUP BY DATE_FORMAT(order_purchase_timestamp, '%Y-%m')
ORDER BY month;
```

### 3.6 这一阶段你要掌握什么

- `GROUP BY`
- `ORDER BY`
- `DATE_FORMAT`
- 条件聚合

---

## 4. 第四阶段：两表和三表分析，正式进入业务题

这一阶段是你最该花时间的地方。

---

### 4.1 orders + customers：用户分布

#### 各州客户数

```sql
SELECT
    c.customer_state,
    COUNT(DISTINCT c.customer_unique_id) AS customer_cnt
FROM olist_orders_dataset o
JOIN olist_customers_dataset c
    ON o.customer_id = c.customer_id
GROUP BY c.customer_state
ORDER BY customer_cnt DESC;
```

#### 各州订单数

```sql
SELECT
    c.customer_state,
    COUNT(DISTINCT o.order_id) AS order_cnt
FROM olist_orders_dataset o
JOIN olist_customers_dataset c
    ON o.customer_id = c.customer_id
GROUP BY c.customer_state
ORDER BY order_cnt DESC;
```

这里要开始注意：

- 用户分析优先用 `customer_unique_id`
- 订单分析用 `order_id`

---

### 4.2 orders + reviews：评分分布

```sql
SELECT
    r.review_score,
    COUNT(*) AS review_cnt
FROM olist_order_reviews_dataset r
GROUP BY r.review_score
ORDER BY r.review_score;
```

#### 已交付订单的平均评分

```sql
SELECT
    AVG(r.review_score) AS avg_review_score
FROM olist_orders_dataset o
JOIN olist_order_reviews_dataset r
    ON o.order_id = r.order_id
WHERE o.order_status = 'delivered';
```

---

### 4.3 orders + order_items：GMV 和客单价

#### 商品金额口径 GMV

```sql
SELECT
    SUM(price) AS gmv_price_only
FROM olist_order_items_dataset;
```

#### 含运费口径 GMV

```sql
SELECT
    SUM(price + freight_value) AS gmv_with_freight
FROM olist_order_items_dataset;
```

#### 平均订单金额

```sql
WITH order_amount AS (
    SELECT
        order_id,
        SUM(price + freight_value) AS order_total
    FROM olist_order_items_dataset
    GROUP BY order_id
)
SELECT
    AVG(order_total) AS avg_order_value
FROM order_amount;
```

---

### 4.4 orders + order_items + products：品类分析

#### 品类销量 Top 10

```sql
SELECT
    p.product_category_name,
    COUNT(*) AS item_cnt
FROM olist_order_items_dataset i
JOIN olist_products_dataset p
    ON i.product_id = p.product_id
GROUP BY p.product_category_name
ORDER BY item_cnt DESC
LIMIT 10;
```

#### 英文品类名 Top 10

```sql
SELECT
    t.product_category_name_english,
    COUNT(*) AS item_cnt
FROM olist_order_items_dataset i
JOIN olist_products_dataset p
    ON i.product_id = p.product_id
LEFT JOIN product_category_name_translation t
    ON p.product_category_name = t.product_category_name
GROUP BY t.product_category_name_english
ORDER BY item_cnt DESC
LIMIT 10;
```

### 4.5 这一阶段你要掌握什么

- 两表 join
- 三表 join
- 用户粒度与订单粒度区别
- GMV 口径定义
- 维度分析

---

## 5. 第五阶段：窗口函数和用户复购分析

这一阶段是你从“会 SQL”到“像分析师一样写 SQL”的分水岭。

### 5.1 每个用户第一次下单时间

```sql
SELECT
    c.customer_unique_id,
    MIN(o.order_purchase_timestamp) AS first_order_time
FROM olist_orders_dataset o
JOIN olist_customers_dataset c
    ON o.customer_id = c.customer_id
GROUP BY c.customer_unique_id;
```

### 5.2 每个用户最后一次下单时间

```sql
SELECT
    c.customer_unique_id,
    MAX(o.order_purchase_timestamp) AS last_order_time
FROM olist_orders_dataset o
JOIN olist_customers_dataset c
    ON o.customer_id = c.customer_id
GROUP BY c.customer_unique_id;
```

### 5.3 复购用户占比

```sql
WITH user_order_cnt AS (
    SELECT
        c.customer_unique_id,
        COUNT(DISTINCT o.order_id) AS order_cnt
    FROM olist_orders_dataset o
    JOIN olist_customers_dataset c
        ON o.customer_id = c.customer_id
    GROUP BY c.customer_unique_id
)
SELECT
    COUNT(*) AS total_users,
    SUM(CASE WHEN order_cnt >= 2 THEN 1 ELSE 0 END) AS repurchase_users,
    ROUND(
        SUM(CASE WHEN order_cnt >= 2 THEN 1 ELSE 0 END) / COUNT(*),
        4
    ) AS repurchase_rate
FROM user_order_cnt;
```

### 5.4 使用窗口函数给订单编号

```sql
WITH user_orders AS (
    SELECT
        c.customer_unique_id,
        o.order_id,
        o.order_purchase_timestamp,
        ROW_NUMBER() OVER (
            PARTITION BY c.customer_unique_id
            ORDER BY o.order_purchase_timestamp
        ) AS order_rank
    FROM olist_orders_dataset o
    JOIN olist_customers_dataset c
        ON o.customer_id = c.customer_id
)
SELECT *
FROM user_orders
WHERE order_rank <= 3
LIMIT 20;
```

### 5.5 这一阶段你要掌握什么

- `MIN` / `MAX`
- 用户生命周期
- `ROW_NUMBER() OVER (...)`
- 复购分析

---

## 6. 第六阶段：物流、评分、支付做综合分析

这是最接近真实业务分析的阶段。

### 6.1 延迟交付率

```sql
SELECT
    COUNT(*) AS delivered_orders,
    SUM(
        CASE
            WHEN order_delivered_customer_date > order_estimated_delivery_date THEN 1
            ELSE 0
        END
    ) AS delayed_orders,
    ROUND(
        SUM(
            CASE
                WHEN order_delivered_customer_date > order_estimated_delivery_date THEN 1
                ELSE 0
            END
        ) / COUNT(*),
        4
    ) AS delayed_rate
FROM olist_orders_dataset
WHERE order_status = 'delivered'
  AND order_delivered_customer_date IS NOT NULL;
```

### 6.2 配送时长和评分关系

```sql
WITH delivery_review AS (
    SELECT
        o.order_id,
        TIMESTAMPDIFF(
            DAY,
            o.order_purchase_timestamp,
            o.order_delivered_customer_date
        ) AS delivery_days,
        r.review_score
    FROM olist_orders_dataset o
    JOIN olist_order_reviews_dataset r
        ON o.order_id = r.order_id
    WHERE o.order_status = 'delivered'
      AND o.order_delivered_customer_date IS NOT NULL
)
SELECT
    review_score,
    AVG(delivery_days) AS avg_delivery_days,
    COUNT(*) AS order_cnt
FROM delivery_review
GROUP BY review_score
ORDER BY review_score;
```

### 6.3 不同支付方式的订单数和金额

```sql
SELECT
    payment_type,
    COUNT(DISTINCT order_id) AS order_cnt,
    SUM(payment_value) AS total_payment
FROM olist_order_payments_dataset
GROUP BY payment_type
ORDER BY total_payment DESC;
```

### 6.4 不同卖家的销售额

```sql
SELECT
    seller_id,
    SUM(price + freight_value) AS seller_gmv,
    COUNT(DISTINCT order_id) AS order_cnt
FROM olist_order_items_dataset
GROUP BY seller_id
ORDER BY seller_gmv DESC
LIMIT 20;
```

---

## 7. 第七阶段：写一份完整分析报告

当你走到这一步，才算这个项目真正闭环。

建议报告结构：

1. 项目背景
2. 数据源说明
3. 表结构与主链路
4. 数据导入与清洗方法
5. 数据质量检查
6. 核心业务分析
7. 结论与建议

### 7.1 报告里至少要放的 8 个问题

1. 总订单量是多少
2. 月度订单趋势如何
3. 各订单状态分布如何
4. GMV 是多少
5. 客单价是多少
6. 用户复购率是多少
7. 哪些品类卖得最好
8. 延迟交付是否影响评分

### 7.2 可直接复用的报告开头

```md
# Olist 巴西电商数据分析报告

## 1. 项目目标

本项目基于 Kaggle 的 Olist 巴西电商数据集，完成 CSV 数据导入 MySQL、数据质量审计、SQL 业务分析与分析报告输出，重点训练关系型多表分析能力。

## 2. 数据说明

核心表包括订单表、订单商品表、支付表、评价表、客户表、商品表、卖家表以及品类翻译表。数据天然具有一对多、多表关联特征，适合训练订单、用户、商品、支付、物流等多维分析。
```

---

## 8. 你接下来 3 周的具体安排

这个安排更贴合你当前状态。

### 第 1 周：只做“导入可信 + 审计”

每天任务：

- 跑导入脚本
- 检查表、行数、主键、外键连接情况
- 检查空值、时间逻辑、金额对账
- 把每条检查 SQL 保存下来

本周产出：

- 一份“导入验收 SQL 清单”
- 一份“数据质量问题清单”

### 第 2 周：刷单表、双表、三表 SQL

每天任务：

- 5 条基础题：单表统计、时间趋势、状态分布
- 5 条 join 题：订单-客户、订单-支付、订单-评价
- 3 条三表题：订单-商品-品类 / 订单-商品-卖家

本周产出：

- 至少 30 条你自己写过并跑通过的 SQL

### 第 3 周：做专题分析和报告

每天任务：

- 用户复购专题
- 品类与卖家专题
- 物流与评价专题
- 支付方式专题
- 最后整理成 Markdown 报告

本周产出：

- 一份完整项目报告
- 一份可以讲给别人听的项目复盘

---

## 9. 现阶段最应该补的能力

按优先级排序：

1. 粒度意识
2. 口径意识
3. 多表 join 控制
4. 条件聚合
5. 时间分析
6. 窗口函数
7. 报告表达

你现在的薄弱点大概率不是 Python 导入，而是：

- 一对多 join 后指标重复
- 不清楚什么时候该先聚合再 join
- 不清楚用户分析该用 `customer_id` 还是 `customer_unique_id`
- 不清楚 GMV、订单数、客单价分别该从哪张表取

所以你后面练习时，每做一道题都先写一句：

```text
这道题的分析粒度是什么？
指标从哪张表出？
join 后会不会重复？
```

---

## 10. 最短闭环路线

如果你现在不想铺太开，直接走这条最短路线：

1. 跑导入
2. 做 10 条数据审计 SQL
3. 做 20 条基础业务 SQL
4. 做 5 条综合分析 SQL
5. 写一份 Markdown 报告

做到这一步，这个项目已经能作为一段完整项目经历讲出来。

---

## 11. 下一步最建议你立刻开始写的 10 条 SQL

1. `olist_orders_dataset` 总订单数
2. 各订单状态分布
3. 月度订单趋势
4. 月度取消率
5. `order_items` 口径 GMV
6. `payments` 口径订单支付总额
7. 金额对账差异订单
8. 各州客户数
9. 品类销量 Top 10
10. 延迟交付率

先把这 10 条跑通，再继续扩展到 30 条、50 条。
